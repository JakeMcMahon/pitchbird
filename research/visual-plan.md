# Visual plan — Pitchbird one-pager lead magnet v5

10 pages. Each visual must serve the on-topic narrative. Drop anything decorative.

## Page-by-page

### Page 1 — Cover
- Type-led, already approved. **Reuse current.**
- Optional: small abstract "stack of single sheets" graphic in the navy quote band.

### Page 2 — Why the one-pager matters
- **HERO**: a one-page document being scanned in a Z-shape by an investor's gaze. Eye-track lines overlaid in gold. Navy bg.
- **Stat card**: 32% / 15s (Storydoc). Already exists, keep.
- Add: a second stat from research findings if available.

### Page 3 — 8 must-haves
- Each must-have gets a Phosphor icon (matching the framework piece):
  1. Company name & logo → `identification-card`
  2. Contact info → `phone`
  3. Company profile → `info`
  4. Business Idea → `lightbulb`
  5. USP → `medal`
  6. Roadmap & Timeline → `chart-line-up` or `path`
  7. Investment ask → `currency-circle-dollar`
  8. Investment highlights → `sparkle`
- Layout: current 2-column grid with icon replacing the number, or icon + number combined.

### Page 4 — 4 nice-to-haves (callouts)
- **Reuse current callout style**. Already great.
- Icons already mapped (target / lightbulb / trend-up / globe).

### Page 5 — Anatomy / Z-scan
- **Keep CSS-built anatomy map** (user explicitly liked this).
- Add a subtle gold arrow tracing the Z-pattern across the map. Sequential numbering already there.

### Page 6 — Design principles in practice
- **Visual do/don't strip**: two miniature one-pager mockups side-by-side. Left "Don't" — text-heavy, no hierarchy. Right "Do" — clean, hierarchy. Built in CSS for control.

### Page 7 — Real-world examples (depends on research)
- If researcher finds Airbnb/Uber/Stripe historical teasers: stylized recreations.
- If not: skip and use a "What strong one-pagers do well" framework page instead.

### Page 8 — Measure & iterate
- 5 analytics (CTR / time on page / scroll depth / conversion / A-B) as callout cards.
- Pair with 4 optimisation moves (drop from 8 → 4 most leveraged) underneath.

### Page 9 — Failure modes
- **Generate AI illustration**: one-pager document slightly crumpled / fading at the edges, "lost in the inbox" feel. Navy bg. Or use a CSS-built "8 red Xs over common mistakes" visual.
- 5-6 specific failures with one-line fixes.

### Page 10 — Trade-offs + CTA
- **Reuse current page 10**: strengths/limitations + pull-quote + gold CTA bar.

## Generation queue

| File | Method | Status |
|---|---|---|
| page2-zscan-hero.png | gpt-5-image | TODO |
| page9-failure-modes.png | gpt-5-image | TODO |
| All Phosphor icons (page 3, 8, etc.) | curl from jsdelivr | TODO |

## CSS-built visuals (no AI needed)
- Page 5: anatomy + gold Z-arrow overlay
- Page 6: do/don't mini-mockups
- Cover quote band: existing
